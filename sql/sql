// for reference - these will not be the final values

CREATE TABLE lbg_epc_complete(
    date timestamp with time zone default now(),
    job_id text PRIMARY KEY,
    agreement_ref text,
    failed_check text,
    client_name text,
    new_product_type text,
    rectify_reason text, 
    complete int DEFAULT -1
);