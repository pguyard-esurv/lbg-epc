CREATE TABLE lbg_epc_complete(
    date timestamp with time zone default now(),
    z_ref int PRIMARY KEY,
    full_name text,
    email_address text,
    phone_number text,
    address text,
    api_call text,
    signature_data BYTEA,
    complete int DEFAULT -1
);